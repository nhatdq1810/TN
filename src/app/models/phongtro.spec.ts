/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Phongtro} from './phongtro';

describe('Phongtro', () => {
  it('should create an instance', () => {
    expect(new Phongtro()).toBeTruthy();
  });
});
