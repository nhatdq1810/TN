/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Giaodich} from './giaodich';

describe('Giaodich', () => {
  it('should create an instance', () => {
    expect(new Giaodich()).toBeTruthy();
  });
});
