/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Nganhang} from './nganhang';

describe('Nganhang', () => {
  it('should create an instance', () => {
    expect(new Nganhang()).toBeTruthy();
  });
});
